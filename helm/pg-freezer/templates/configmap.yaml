apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pg-freezer.fullname" . }}-config
  labels:
    {{- include "pg-freezer.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | nindent 4 }}
